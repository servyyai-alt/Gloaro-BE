const { body } = require("express-validator");

// Placeholders for future validations
const createMemberValidation = [];
const createMinutesValidation = [];
const createDocumentValidation = [];

module.exports = {
  createMemberValidation,
  createMinutesValidation,
  createDocumentValidation,
};
